import 'package:flutter/material.dart';

class BangumiTimelinePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: new AppBar(
        title: new Text("时间表页面"),
      ),
    );
  }
}
